services:
  graphdb:
    image: ontotext/graphdb:10.8.8
    container_name: cipa-graphdb
    ports:
      - "7200:7200"
    volumes:
      - ./graphdb/data:/opt/graphdb/home/data
      - ./graphdb/repositories:/opt/graphdb/home/repositories
    environment:
      GDB_JAVA_OPTS: "-Xmx2g -Xms1g"
