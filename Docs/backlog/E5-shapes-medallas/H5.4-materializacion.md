# H5.4 — Materialización

**Épica:** [E5 — Shapes de medallas](README.md)
**Requisito:** 5 (prepara 6.b) · **Prioridad:** `[SHOULD]` · **Estado:** Pendiente
**Depende de:** H5.3 · **Habilita:** H6.5, H6.6

## Historia de usuario
Como equipo, queremos escribir `pbl:earnedBadge` a partir del resultado de validación, para poder consultar las medallas obtenidas directamente en E6.

## Criterios de aceptación
- [ ] Un `CONSTRUCT` (o regla) deriva `persona pbl:earnedBadge badge` para cada `cipa:Persona` **conforme** a la `BadgeShape` correspondiente.
- [ ] La materialización es **idempotente** y consistente con el resultado de H5.3.
- [ ] Queda documentado el flujo: validar → construir → cargar en GraphDB.

## Tareas
- [ ] Escribir el `CONSTRUCT` que invierte la lógica (genera `earnedBadge` para los conformes).
- [ ] Cargar el resultado en GraphDB.

## Notas de modelado
- Materializar simplifica H6.5/H6.6 (consultar `earnedBadge` directo) y permite que la precedencia de H5.2 se apoye en `earnedBadge`.
- Si no se materializa, las consultas de E6 deben replicar el criterio del shape; documentar la opción elegida.

## Artefactos
- `shapes/materialize-badges.rq` · datos `pbl:earnedBadge`
